package com.dtolabs.bamboo.plugin.rundeck.actions;
 
/**
 * WebWork Action that handles the 'browse' operations in the Rundeck admin interface.  This class has no
 * specific behaviour as the superclass implements the necessary logic.
 * 
 * @author Chuck Scott
 */
public class BrowseRundeckConfig extends RundeckActionSupport {
 
}
